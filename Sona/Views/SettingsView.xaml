<UserControl x:Class="Sona.Views.SettingsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Sona.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <StackPanel Margin="20">
        <TextBlock Text="Setting" FontSize="36"
                   FontFamily="Cascadia Mono"
                   Margin="0,0,0,20"/>
        <StackPanel MaxWidth="400">
            <TextBlock Text="{Binding Settings.MasterVolume, StringFormat=全体の音量: {0}%}" FontSize="12"/>
            <Slider Value="{Binding Settings.MasterVolume, UpdateSourceTrigger=PropertyChanged}"
                    Minimum="0" Maximum="100"
                    Width="400"
                    IsSnapToTickEnabled="True"
                    TickFrequency="1"
                    Margin="0,0,0,10"
                    IsMoveToPointEnabled="True"/>
        </StackPanel>
    </StackPanel>
</UserControl>
